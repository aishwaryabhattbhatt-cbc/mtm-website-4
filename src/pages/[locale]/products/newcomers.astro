---
import Layout from '../../../layouts/Layout.astro';
import { toAbsoluteUrl } from '../../../lib/i18n/routing';
import { buildBreadcrumbList } from '../../../lib/content/breadcrumbs';
import Navbar from '../../../components/shared/Navbar.astro';
import ProductsHeroSection, {
    type HeroBadge,
} from '../../../components/products/ProductsHeroSection.astro';
import NewcomersFeaturesSection from '../../../components/products/NewcomersFeaturesSection.astro';
import NewcomersToolsSection from '../../../components/products/NewcomersToolsSection.astro';
import NewcomersReportsSection from '../../../components/products/NewcomersReportsSection.astro';
import ProductMethodologySection from '../../../components/products/ProductMethodologySection.astro';
import NewcomersExploreMoreSection from '../../../components/products/NewcomersExploreMoreSection.astro';
import HomeFooterSection from '../../../components/home/HomeFooterSection.astro';
import { getPageDictionary, getRuntimeCMSConfig } from '../../../lib/cms/getPageContent';
import { t } from '../../../lib/i18n/t';
import type { Locale } from '../../../lib/cms/types';
import { icons } from '../../../lib/icons/registry';

export function getStaticPaths() {
    return [{ params: { locale: 'en' } }, { params: { locale: 'fr' } }];
}

const locale = (Astro.params.locale === 'fr' ? 'fr' : 'en') as Locale;
const pageId = 'newcomers';
const copy = await getPageDictionary(pageId);
const cmsConfig = getRuntimeCMSConfig(pageId);
const base = import.meta.env.BASE_URL;
const resolvedCmsCsvUrl =
    cmsConfig.csvUrl ??
    (cmsConfig.sheetId && cmsConfig.gid
        ? `https://docs.google.com/spreadsheets/d/${cmsConfig.sheetId}/export?format=csv&gid=${cmsConfig.gid}`
        : '');

const localeSwitchUrls = {
    en: `${base}en/products/newcomers/`,
    fr: `${base}fr/products/newcomers/`,
};

// Positions/sizes read off the reference mock
// (public/images/products/mtm-newcomers/nc.png), as % of the hero media
// box — approximate, to be fine-tuned later. The 2 lower badges sit in
// front of the subjects in the mock (not behind), so they go in
// heroBadgesTop instead of heroBadgesMasked. Icons are placeholders — swap
// any `icon` value for a different registry entry whenever the real set is
// decided.
const heroBadgesMasked: HeroBadge[] = [
    { left: 30, top: 29, size: '5rem', icon: icons['device-cellphone'].data },
    { left: 50, top: 19, size: '5.5rem', icon: icons['device-tv'].cable },
    { left: 72, top: 37, size: '6.5rem', icon: icons['demographics-household'].online },
];
const heroBadgesTop: HeroBadge[] = [
    { left: 23, top: 62, size: '6rem', icon: icons['device-tv'].paid },
    { left: 78, top: 62, size: '5rem', icon: icons.gen.coastToCoast },
];

const pageTitle = t(
    copy,
    'newcomers_page_title',
    locale,
    'MTM Newcomers | Newcomer Media & Technology Data'
);
const pageDescription = t(
    copy,
    'newcomers_page_description',
    locale,
    'Data on media and technology habits of newcomers to Canada.'
);
---

<Layout
    title={pageTitle}
    description={pageDescription}
    locale={locale}
    alternates={localeSwitchUrls}
>
    <main
        data-locale={locale}
        data-cms-page-id={pageId}
        data-cms-csv-url={resolvedCmsCsvUrl}
        data-cms-sheet-id={cmsConfig.sheetId ?? ''}
        data-cms-tab-gid={cmsConfig.gid ?? ''}
        data-cms-refresh-ms={String(cmsConfig.refreshMs)}
    >
        <Navbar locale={locale} copy={copy} localeSwitchUrls={localeSwitchUrls} />

        <ProductsHeroSection
            locale={locale}
            copy={copy}
            ctaHref={`${base}${locale}/request-demo/`}
            heroImageSrc={`${base}images/products/mtm-newcomers/hero/hero-product-nc.webp`}
            maskImageSrc={`${base}images/products/mtm-newcomers/hero/hero-product-nc-mask.webp`}
            heroBadgesMasked={heroBadgesMasked}
            heroBadgesTop={heroBadgesTop}
            heroIconSrc={`${base}${icons['product-newcomers'].badge}`}
            heroIconAlt=""
        />

        <NewcomersFeaturesSection locale={locale} copy={copy} />

        <NewcomersToolsSection locale={locale} copy={copy} />

        <NewcomersReportsSection locale={locale} copy={copy} />

        <ProductMethodologySection
            locale={locale}
            copy={copy}
            showComposition={false}
            providerLogoSrc={`${base}images/products/mtm-newcomers/leger_logo.png`}
        />

        <NewcomersExploreMoreSection locale={locale} copy={copy} />

        <HomeFooterSection locale={locale} copy={copy} />
    </main>

    <Fragment slot="head">
        <script
            is:inline
            type="application/ld+json"
            set:html={JSON.stringify({
                '@context': 'https://schema.org',
                '@type': 'WebPage',
                name: pageTitle,
                url: toAbsoluteUrl(Astro.site, localeSwitchUrls[locale]),
            })}
        />
        <script
            is:inline
            type="application/ld+json"
            set:html={JSON.stringify({
                '@context': 'https://schema.org',
                '@type': 'Service',
                name: 'MTM Newcomers',
                url: toAbsoluteUrl(Astro.site, localeSwitchUrls[locale]),
                description: pageDescription,
                provider: { '@id': `${toAbsoluteUrl(Astro.site, base)}#organization` },
                areaServed: { '@type': 'Country', name: 'Canada' },
                serviceType: 'Media and technology research data',
            })}
        />
        <script
            is:inline
            type="application/ld+json"
            set:html={JSON.stringify(
                buildBreadcrumbList(
                    toAbsoluteUrl(Astro.site, base),
                    'MTM Newcomers',
                    toAbsoluteUrl(Astro.site, localeSwitchUrls[locale])
                )
            )}
        />
    </Fragment>

    <Fragment slot="scripts">
        {
            /* Dev only: parses the sheet CSV for the live-reload in
        runtime/script.js. getRuntimeCMSConfig withholds the sheet details in
        production, so that refresh no-ops there and this was 45KB fetched from
        a CDN for nothing on every page load. */
        }
        {
            import.meta.env.DEV && (
                <script
                    is:inline
                    src="https://cdn.jsdelivr.net/npm/papaparse@5.4.1/papaparse.min.js"
                />
            )
        }
        <script is:inline src={`${base}js/runtime/script.js`}></script>
    </Fragment>
</Layout>
