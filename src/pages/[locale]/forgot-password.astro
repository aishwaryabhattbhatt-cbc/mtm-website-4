---
import Layout from '../../layouts/Layout.astro';
import Navbar from '../../components/shared/Navbar.astro';
import ForgotPasswordHeroSection from '../../components/forgot-password/ForgotPasswordHeroSection.astro';
import HomeFooterSection from '../../components/home/HomeFooterSection.astro';
import SignInBackgroundScripts from '../../components/sign-in/SignInBackgroundScripts.astro';
import { getPageDictionary } from '../../lib/cms/getPageContent';
import { getLocaleSwitchUrls } from '../../lib/i18n/routing';
import { t } from '../../lib/i18n/t';
import type { Locale } from '../../lib/cms/types';

export function getStaticPaths() {
    return [{ params: { locale: 'en' } }, { params: { locale: 'fr' } }];
}

const locale = (Astro.params.locale === 'fr' ? 'fr' : 'en') as Locale;
const copy = await getPageDictionary('forgot-password');
const base = import.meta.env.BASE_URL;
const localeSwitchUrls = getLocaleSwitchUrls(base, 'forgot-password');

const pageTitle = t(copy, 'forgot_password_page_title', locale, 'MTM | Forgot Password');
const pageDescription =
    locale === 'fr'
        ? 'Réinitialisez le mot de passe de votre compte Media Technology Monitor (MTM).'
        : 'Reset the password for your Media Technology Monitor (MTM) account.';
---

<Layout
    title={pageTitle}
    description={pageDescription}
    locale={locale}
    alternates={localeSwitchUrls}
    noindex={true}
>
    <main class="forgot-password-page">
        <Navbar locale={locale} copy={copy} localeSwitchUrls={localeSwitchUrls} />

        <ForgotPasswordHeroSection locale={locale} copy={copy} />

        <HomeFooterSection
            locale={locale}
            copy={copy}
            showCta={false}
            showNewsletter={false}
            showBgLeftCenter={false}
        />
    </main>

    <Fragment slot="scripts">
        <SignInBackgroundScripts />
    </Fragment>
</Layout>

<style>
    .forgot-password-page {
        width: 100%;
    }
</style>
