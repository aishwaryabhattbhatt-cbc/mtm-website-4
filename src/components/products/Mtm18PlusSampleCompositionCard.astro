---
import Mtm18PlusDonutChart from './Mtm18PlusDonutChart.astro';

interface DonutItem {
    label: string;
    percentage: string;
}

interface Props {
    pillLabel: string;
    iconSrc: string;
    items: DonutItem[];
}

const { pillLabel, iconSrc, items } = Astro.props as Props;
---

<div class="composition-card">
    <div class="composition-card-pill">
        <img
            src={iconSrc}
            alt=""
            aria-hidden="true"
            class="icon-xl composition-card-icon"
            loading="lazy"
            decoding="async"
        />
        <span class="text-label-regular composition-card-pill-label">{pillLabel}</span>
    </div>

    <div class="composition-card-donuts">
        {items.map((item) => <p class="text-body-p1-bold composition-donut-label">{item.label}</p>)}
        {items.map((item) => <Mtm18PlusDonutChart percentage={item.percentage} size="12rem" />)}
    </div>

    <p class="text-body-p3 composition-card-description">
        Sample composition is weighted to reflect the national distribution of Canadian households.
    </p>
</div>

<style>
    .composition-card {
        background-color: var(--surface-white);
        border-radius: var(--radius-m);
        box-shadow: var(--shadow-card);
        padding: var(--space-8);
        display: flex;
        flex-direction: column;
        gap: var(--space-8);
    }

    .composition-card-pill {
        display: inline-flex;
        align-items: center;
        gap: var(--space-3);
        background-color: var(--surface-blue);
        border-radius: var(--radius-s);
        padding: var(--space-2) var(--space-4) var(--space-2) var(--space-2);
        align-self: flex-start;
    }

    .composition-card-icon {
        flex-shrink: 0;
    }

    .composition-card-pill-label {
        color: var(--text-secondary);
        white-space: nowrap;
    }

    .composition-card-donuts {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: var(--space-4) var(--space-8);
        justify-items: center;
    }

    .composition-donut-label {
        color: var(--text-primary);
        text-align: center;
        margin: 0;
    }

    .composition-card-description {
        color: var(--text-secondary);
        margin: 0;
    }
</style>
