---
import type { CMSDictionary, Locale } from '../../lib/cms/types';
import InsightsReportsLibrarySection from '../insights/InsightsReportsLibrarySection.astro';
interface Props {
    locale: Locale;
    copy: CMSDictionary;
}
const { locale, copy } = Astro.props;
---

<InsightsReportsLibrarySection locale={locale} copy={copy} instanceId="solutions" />
